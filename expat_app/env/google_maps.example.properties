# Copy this file to google_maps.properties in the same folder (gitignored).
# GOOGLE_MAPS_API_KEY=your_key_here
#
# Geocoding API: enable it under APIs & Services → Library → "Geocoding API" → Enable,
# then add "Geocoding API" to this key's API restrictions (it only appears after enable).

GOOGLE_MAPS_API_KEY=
