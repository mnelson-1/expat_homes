# Examples

`SYNTHETIC_layout_demo_only.jsonl` exists **only** to test chart layout (`python scripts/plot_workflow_timeseries.py --allow-synthetic-layout-demo`). **Do not** present it as measured performance.

Real data belongs in `../workflow_history.jsonl` (see `../README.md`).
